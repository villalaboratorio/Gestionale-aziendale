// Importare le funzioni di utilità
const validation = require('./utils/validation');
const constants = require('./constants');

// Esportare tutte le risorse condivise da un singolo punto
module.exports = {
    validation,
    constants
};
